<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.learn.springboot06datamybatis.mapper.UserMapper">

  <select id="getUserById" resultType="cn.learn.springboot06datamybatis.bean.User">
    SELECT * FROM user WHERE id = #{id}
  </select>
</mapper>